namespace AlloyClient;

public enum ScreenType {
    Menu,
    Game
}

public enum FullscreenType {
    Exclusive,
    Borderless
}

public enum SpeechColors {
    Default,
    Enemy,
    Guild,
    Party,
    Tell
}

public enum UseType {
    DEFAULT = 0,
    START_USE = 1,
    END_USE = 2
}